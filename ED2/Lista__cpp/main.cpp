#include "lista.cpp"

int main(){

    // No x(3);
    // No n(4);

    // n.imprimir();

    // No* z = new No(15);
    // z->imprimir();

    Lista l;
    l.inserir(4);
    l.inserir(40);
    l.inserir(400);
    
    l.imprimir();

    return 0;
}